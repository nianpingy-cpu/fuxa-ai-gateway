{
  "annotations": {
    "list": []
  },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 0, "y": 0 },
      "id": 1,
      "options": { "reduceOptions": { "values": false, "calcs": ["lastNotNull"] } },
      "targets": [{ "expr": "fuxa_request_count", "legendFormat": "requests", "refId": "A" }],
      "title": "Request Count",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "unit": "ms" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 6, "y": 0 },
      "id": 2,
      "options": { "reduceOptions": { "values": false, "calcs": ["lastNotNull"] } },
      "targets": [{ "expr": "fuxa_latency_ms_total", "legendFormat": "latency", "refId": "A" }],
      "title": "Total Latency",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 0 },
      "id": 3,
      "options": { "reduceOptions": { "values": false, "calcs": ["lastNotNull"] } },
      "targets": [{ "expr": "fuxa_error_count", "legendFormat": "errors", "refId": "A" }],
      "title": "Error Count",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "unit": "short" },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 0 },
      "id": 4,
      "options": { "reduceOptions": { "values": false, "calcs": ["lastNotNull"] } },
      "targets": [{ "expr": "sum(fuxa_tool_usage)", "legendFormat": "tool calls", "refId": "A" }],
      "title": "Tool Usage",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 5,
      "targets": [
        {
          "expr": "fuxa_tool_usage",
          "legendFormat": "{{tool}}",
          "refId": "A"
        }
      ],
      "title": "Tool Usage by Tool",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["fuxa", "mcp", "gateway"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "FUXA AI Gateway",
  "uid": "fuxa-ai-gateway",
  "version": 1
}
